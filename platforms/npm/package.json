{
  "name": "oneai-cli",
  "version": "0.1.0",
  "description": "Cross-platform launcher for the OneAI engine (oneai-cli TUI + subcommands). Postinstall fetches the platform binary from GitHub Releases; the bin forwards argv + stdio with zero business logic. Falls back to `oneai` on PATH if no binary is downloaded (e.g. a dev who `cargo install`-ed).",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Marssssss/OneAI",
    "directory": "platforms/npm"
  },
  "bin": {
    "oneai": "bin/oneai.js"
  },
  "scripts": {
    "postinstall": "node install.js"
  },
  "files": [
    "bin/oneai.js",
    "install.js",
    "README.md"
  ],
  "engines": {
    "node": ">=16"
  }
}

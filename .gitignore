# Rust
/target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~


# macOS
.DS_Store

# SWE-bench evaluation artifacts — cloned source repos, prediction JSONL,
# and Modal/local report files. Generated by scripts/swebench/ runs.
# Add additional cloned repo dirs here as needed.
astropy/
predictions*.jsonl
*.prediction.jsonl
oneai.*.json
evaluation_results/
logs/
# Eval workspace: cloned repos (each is its own git repo — must never be
# tracked, or they land as corrupt embedded gitlinks) + per-run artifacts.
swebench-workspace/
# Downloaded dataset (export_dataset.py output — 300 rows, regenerable).
swe_bench_lite.jsonl
swe_bench_verified.jsonl
